mutation CreateCompanyLocation($input: CreateCompanyLocationInput!) {
  createCompanyLocation(input: $input) {
    id
    address
    city
    country
    postalCode
    county
    contact
    companyId
  }
}
