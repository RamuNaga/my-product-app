# company-prisma

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build company-prisma` to build the library.

## Running unit tests

Run `nx test company-prisma` to execute the unit tests via [Jest](https://jestjs.io).
