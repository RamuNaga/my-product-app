# user-grpc-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build user-grpc-client` to build the library.

## Running unit tests

Run `nx test user-grpc-client` to execute the unit tests via [Jest](https://jestjs.io).
