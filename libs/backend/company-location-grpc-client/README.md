# company-location-grpc-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build company-location-grpc-client` to build the library.

## Running unit tests

Run `nx test company-location-grpc-client` to execute the unit tests via [Jest](https://jestjs.io).
