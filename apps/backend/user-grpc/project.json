{
  "name": "user-grpc",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend/user-grpc/src",
  "projectType": "application",
  "tags": ["type:grpc", "domain:user", "scope:backend"],
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{workspaceRoot}/dist/apps/backend/user-grpc"],
      "options": {
        "webpackConfig": "apps/backend/user-grpc/webpack.config.js",
        "outputPath": "dist/apps/backend/user-grpc",
        "watch": false
      },
      "configurations": {
        "development": {
          "webpackConfig": "apps/backend/user-grpc/webpack.config.js",
          "outputPath": "dist/apps/backend/user-grpc"
        },
        "production": {
          "webpackConfig": "apps/backend/user-grpc/webpack.config.js",
          "outputPath": "dist/apps/backend/user-grpc"
        }
      }
    },

    "copy-protos": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx copyfiles -u 5 \"libs/backend/proto/src/lib/user.proto\" \"dist/apps/backend/user-grpc/\""
      }
    },

    "build-with-protos": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run user-grpc:build && nx run user-grpc:copy-protos"
      }
    },

    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build-with-protos"],
      "options": {
        "buildTarget": "user-grpc:build-with-protos",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "user-grpc:build-with-protos"
        },
        "production": {
          "buildTarget": "user-grpc:build-with-protos"
        }
      }
    },

    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/backend/user-grpc/jest.config.ts",
        "passWithNoTests": true
      }
    },

    "prune-lockfile": {
      "dependsOn": ["build-with-protos"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/backend/user-grpc/package.json",
        "{workspaceRoot}/dist/apps/backend/user-grpc/pnpm-lock.yaml"
      ],
      "options": {
        "buildTarget": "build-with-protos"
      }
    },

    "copy-workspace-modules": {
      "dependsOn": ["build-with-protos"],
      "cache": true,
      "outputs": [
        "{workspaceRoot}/dist/apps/backend/user-grpc/workspace_modules"
      ],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build-with-protos"
      }
    },

    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    }
  }
}
